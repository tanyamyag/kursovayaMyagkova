// vite.config.js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// ✅ Конфигурация Vite для Vue 3 и деплоя на Vercel
export default defineConfig({
  // Подключаем Vue-плагин
  plugins: [vue()],

  // ⚙️ Настройки сервера для локальной разработки
  server: {
    host: true,   // позволяет открывать сайт по локальной сети
    port: 5173,   // стандартный порт Vite
    open: true    // автоматически открывает браузер при npm run dev
  },

  // 🏗️ Настройки сборки
  build: {
    outDir: 'dist',     // куда складывать результат сборки
    sourcemap: false,   // не создаём карты исходников
    minify: 'terser',   // минификация через Terser
    chunkSizeWarningLimit: 1000 // не ругается на крупные чанки
  },

  // 🧩 ВАЖНО: относительные пути для корректной работы на Vercel и GitHub Pages
  base: './'
})
